# Audrey portfolio website

## Change these before publishing
1. Replace `REPLACE_WITH_AUDREYS_EMAIL` in `index.html`.
2. Add the CV as `assets/documents/Audrey-Aghaji-CV.pdf`.
3. Replace all links beginning with `REPLACE_WITH_`.
4. Add the university and completion year under Master of Public Health.
5. Replace or delete the placeholder certificate card.
6. Confirm Audrey's exact contribution to each project.
7. Do not upload confidential or internal government material.

## Test
Double-click `index.html`.

## Publish with GitHub Pages
1. Create a GitHub account.
2. Create a public repository named exactly `YOUR-USERNAME.github.io`.
3. Upload these files, keeping `index.html` at the top level.
4. Open repository **Settings → Pages**.
5. Choose **Deploy from a branch**.
6. Select **main** and **/(root)**, then save.
7. Visit `https://YOUR-USERNAME.github.io`.
